<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <ProgressBar
            android:id="@+id/profileProgressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="visible" />

        <LinearLayout
            android:id="@+id/profileContent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone">

            <ImageView
                android:id="@+id/ivUserProfile"
                android:layout_width="150dp"
                android:layout_height="150dp"
                android:layout_gravity="center_horizontal"
                android:layout_marginBottom="16dp"
                android:scaleType="centerCrop"
                android:src="@drawable/ic_launcher_background" />

            <TextView android:id="@+id/tvProfileName" style="@style/ProfileTextView" />
            <TextView android:id="@+id/tvProfileEmail" style="@style/ProfileTextView" />
            <TextView android:id="@+id/tvProfilePhone" style="@style/ProfileTextView" />
            <TextView android:id="@+id/tvProfileLocation" style="@style/ProfileTextView" />
            <TextView android:id="@+id/tvProfileDob" style="@style/ProfileTextView" />
            <TextView android:id="@+id/tvProfileGender" style="@style/ProfileTextView" />

            <Button
                android:id="@+id/btnGoToEditProfile"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                android:text="Edit Profile" />
        </LinearLayout>
    </LinearLayout>
</ScrollView>